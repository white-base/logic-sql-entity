/**** sql-row.js | SQLRow ****/
//==============================================================
import { MetaRow } from 'logic-entity';

class SQLRow extends MetaRow {
  constructor(p_entity) {
    super(p_entity);
  }
}


export default SQLRow;
export { SQLRow };