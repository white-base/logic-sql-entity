/**** sql-set.js | SQLSet ****/
//==============================================================
import { MetaSet } from 'logic-entity';

class SQLSet extends MetaSet {
    constructor(p_entity) {
        super(p_entity);
    }
}


export default SQLSet;
export { SQLSet };