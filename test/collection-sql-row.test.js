import { jest } from '@jest/globals';

// import { SQLRowCollection } from '../src/collection-sql-row.js';

//==============================================================
// test
describe("[target: sql-row-collection.js]", () => {
    describe("SQLRowCollection :: 클래스", () => {
        beforeEach(() => {
            jest.resetModules();
            // MetaRegistry.init();
        });
        describe("<테이블 등록후 속성 검사>", () => {
            it("- 테이블 등록후 속성 검사 ", () => {
                
            });
        });
    });
});